{% extends 'base.html.twig' %}

{% block body %}
<div class="container">
    <div class="row">
        <a class="btn btn-success" href="{{ path('app_user_novy_tym') }}" role="button"><i class="fas fa-plus"></i> Nový tým</a>
    </div>
    <div class="row">
        {%  if tymy|length == 0  %}
        <h4>Zatím nejsi v žádném týmu</h4>
        {% else %}
        <h4>Seznam tvých týmů</h4>
            {{ include('tym/_tymy.html.twig') }}
        {% endif %}
    </div>
</div>

{% endblock %}